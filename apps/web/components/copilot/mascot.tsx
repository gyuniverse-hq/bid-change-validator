/** Reconstructed from the supplied Figma CSS, not an exported image asset.
 * Neutral expression is fixed regardless of judgment or loading state.
 */
export function CopilotMascot({ size = 20 }: { size?: 20 | 64 }) {
  return <span className="copilot-mascot" style={{ width: size, height: size }} aria-hidden="true">
    <span className="copilot-mascot-eye copilot-mascot-eye-left" />
    <span className="copilot-mascot-eye copilot-mascot-eye-right" />
  </span>;
}
