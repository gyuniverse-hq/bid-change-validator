# 파이프라인 실행 결과와 다음 작업

2026-09-16. 작업 브랜치 `codex/ai-copilot-evaluation-ready`, 시작 HEAD `6af3b4f` 이후 미commit 변경을 포함한다.

현재는 **④ 통합 검증의 구현 체크포인트, NOT_READY**다. 실행 중이던 v35 검사는 수집을 마쳤고 추가 모델 검사는 보류했다. 사용자 제안에 따라 자유질문의 반복 보완을 끝없이 계속하기보다, Job·질문 선택형 범위로 전환할 것을 권장한다. 새 선택 UI는 아직 구현하지 않았다. 최종 진입 상태는 `.ci-results/user-evaluation/pipeline-20260916-v03/status.json`으로 확인한다.

| 전체 순서 | 현재 상태 |
|---|---|
| ① 감사 · ② 연결 확인 · ③ 기본 검사 | 기존 이력과 이번 변경의 관련 검증을 보존 |
| ④ 파이프라인 통합 검증 | 현재 코드·성공·실패·미실행 근거 보존. Job·질문 범위 고정 전환 제안 |
| ⑤ 사용자 평가 | 이번 후보에서는 미실시. 선택형 질문 흐름 검증 후 진입 |
| ⑥ 최종 통합 판단 | 사용자 평가와 최신 develop 비교 후 별도 판단 |

이번 구현은 Job 최초 요청/미해결 항목 유지, 같은 출처의 검증 내용 재사용, LangChain의 실제 검색·프롬프트·모델 경로, 단계별 제출 의무, 자연어 제안과 명시 실행, 접힌 진단 표시를 연결한다. 남원 네 회사는 로컬 초기 입력을 보존하고 추가답변 실행은 복사한 사례에만 수행한다.

현재 모델 없는 공통 검사는 **557 passed**다. 기존 전체 백엔드 실행과 수정 범위 재검사를 중복 없이 연결한 결과는 **1039 passed / 11 skipped**이며, 한 번의 전체 성공 실행이 아니다. 타입·production build·기존 프론트 계약·변경 분류 화면도 별도 근거를 남겼다. 이전 “47개 검사” 주장을 소급 확인한 결과가 아니다.

계획한 실제 모델 검증은 남원 4개 Job과 다른 Core 3개 Job, 이후 위험 높은 2개 Job의 재확인이다. 최신 후보의 결과는 아래와 같다. HTTP 200이나 제품의 자체 PASS만으로 통과하지 않으며, 이전 후보의 성공을 최신 합격으로 합산하지 않았다.

| 최신 v35 시나리오 | 결과 | 남은 사항 |
|---|---|---|
| N1 남원 J13 변경 대응 | 해당 3회 흐름의 원문·저장 판정·화면 대조 통과 | 답변 길이와 반복은 별도 개선 대상 |
| N2 남원 J14 반대 영향 | 해당 3회 흐름 대조 통과 | 첫·두 번째 답변 길이 및 지연 |
| N3 남원 J15 예외 설명 | 실패: 4회 후에도 PARTIAL / OPEN | 예외 설명에 처리 완료 후 제출 단계까지 완료 조건으로 붙는 범위 문제 |
| N4 남원 J16 추가답변 | 가정·제안 비저장, 명시 저장, 새 판정·화면 조회 경로 통과 | 기준 변경 후 전체 Job은 OPEN. Job 종료 UX까지 완료된 것은 아님 |
| C1 구내식당 참여 준비 | 실패: 4회 후에도 PARTIAL / OPEN | 등록·계약 단계 서류와 미해결 항목 설명의 완료 판정 |
| C2 우치공원 변경 대응 | 후속 설명은 완료됐으나 전체 품질 검토는 실패로 보존 | 첫 답변 내용과 누락 표시 불일치, 본문의 내부 F 참조 노출·장문 |
| C3 한림대 | 현재 후보 미실행 | 추가 호출 보류 표식에서 시작 전 종료. 제품 실행 실패로 계산하지 않음 |
| G4 위험 흐름 재확인 | 현재 후보 미실행 | G3 미통과로 진입하지 않음 |

N4의 기능 통과와 전체 Job 종료 상태를 구분한다. 통과 행도 해당 데이터·코드의 실행 근거이며, 아직 새 UI의 승인 질문 목록이나 모든 공고의 품질 보증이 아니다. 실제 원문 리뷰는 각 실행 폴더의 `review.json`, 최종 정리는 `checkpoint.json`에 남겼다.

현재 읽기·모델 설명 응답은 관측상 약 15~70초였다. 두 Job 병렬 실행이 포함된 값이고, 로컬 제안·저장 후 조회 시간과 구별한다. 짧은 질문 20초/복합 질문 45초 목표를 일관되게 충족하지 못했으며 성능 통과로 보고하지 않는다.

마지막 읽기 전용 DB 검사는 원래 J13, 남원 네 초기 회사와 판정, 8개 문서 기준, 미사용 사용자 실행 사례의 보존을 확인했다. 기존 성공 기록과 별도로 최종 `readback.json` 시각을 갱신했다.

완료 판정과 별개로 다음 한계는 유지한다.

- 남원 Golden 네 조건 묶음의 충족과 전체 참가 가능은 다르다. 별도 조달 등록 미달이 남는다.
- 업종군 관계는 로컬 v03 검수 가정이다. 원래 Q-009 법적 해석을 전역 확정하지 않았다.
- 다른 Core v02 기대값은 초안이다. Job 설명 검증을 전체 판정 엔진의 정확성 보증으로 확대하지 않는다.
- 날짜가 지난 일정은 과거 이행 확인 대상으로 표시한다. 실제 제출·증빙 확보를 자동 완료하지 않는다.
- 대화 기록의 서버 재시작 복원과 전체 재분석은 이번 사용자 평가 경로에 포함하지 않는다.
- 응답 길이·반복·대기시간은 실제 사용자 평가에서 별도로 확인한다. 작은 표본으로 p95나 전체 품질을 보증하지 않는다.

상세 구현·원인·실행 근거는 [43번 보고서](43-pipeline-implementation-and-verification.md), 보류 중인 평가 순서는 [44번 안내](44-user-evaluation-guide.md), 다음 설계 제안은 [46번 Job 선택형 전환 의견](46-guided-job-ui-transition.md), 추후 공용 DB 매핑에 사용할 회사 입력은 [적용 명세](evaluation-assets/namwon-v03-company-inputs.json)에 정리했다. 비밀번호·API 키·원문 복사본은 Git 산출물에 넣지 않는다.

main/develop 수정, 공용 DB 변경, merge·배포는 수행하지 않았다.
