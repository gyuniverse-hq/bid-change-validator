# 자연어 현장 방문 답변 → 저장 전 제안 복구

2026-09-15 KST. 시작 코드 6230b50. 22번의 초기 원인 검증에 이은 실제 재현·보완 기록.

## 전체 순서

1 감사 → 2 연결 → 3 기본 검사: 기존 수행. 4 통합 검증: Q-009 기관 해석 대기. **5 사용자 평가: 자연어 답변의 저장 전 제안 연결 보완 및 브라우저 재검증 완료.** 사용자 재평가·실행 확인 후 해당 사례 재판정 평가는 남아 있다. 6 최종 통합 판단·merge·배포 미실행.

## 구현 범위

- 명확한 현장 방문/확인서 제출 두 상태 문장만 제한된 전체 문장 문법으로 인식한다. 모든 자연어를 지원하는 모델 추출기가 아니다. 부분 답변·가정·질문·추가 명령·다른 회사 문장은 자동 후보로 변환하지 않는다.
- 직전 대화에서 실제로 제시한 READ_CHECKS 대상과 현재 askable 요건을 교차 확인한다. 해당 현장 방문 계약의 두 필드와 현재 confirmation_basis를 사용한다. 복수 대상·옛 대화 대상·다른 scope는 자동 연결하지 않는다.
- 방문 완료/확인서 미제출을 각각 true/false로 제안한다. 증빙 보유를 추정하지 않는다. 기존 propose_answer가 현재 규칙·원문 hash·필드 완전성·최신성을 검증하며 기존 확인 실행 경로를 유지한다.
- 후보 내용과 미저장 상태는 서버가 직접 확인할 수 있는 절차 사실이다. 이 안내를 모델에 재작성시키지 않고 서버 문장으로 반환한다. 이를 회사 자격의 실모델 검증이라고 부르지 않는다.
- 일반 질문 경로·Q-009·DB 저장 규칙은 변경하지 않았다. 사용자 확인 없이 저장하거나 재판정하지 않는다.

## 실패와 최종 검사

모든 browser 경로는 Git 제외 `.ci-results/user-evaluation/` 아래에 있다.

| 실행 | 실제 결과 |
|---|---|
| browser-2026-09-14T21-11-24-390Z | 수정 전 실제 모델: FAIL, 제안0, PARTIAL. 원인 재현 |
| browser-2026-09-14T21-15-05-140Z | 첫 수정: 제안1·입력값 정확·모델 PASS. 검사기 화면 선택자가 답변2개에 맞지 않아 전체 FAIL |
| browser-2026-09-14T21-16-50-179Z | 선택자 수정 후 재실행: 제안값은 맞으나 설명 모델 PARTIAL. 통과로 처리하지 않음 |
| 최종 오프라인 | **429 PASS**, `.ci-results/copilot-v31-20260914T211939Z/tests.xml` |
| 최종 별도 테스트 DB/API | **60 PASS**, `.ci-results/copilot-db-20260914T212104Z/tests.xml` |
| browser-2026-09-14T21-20-54-187Z | **PASS**. 실제 Chrome 로그인 → 첫 질문 실모델 → 후속 답변 서버 제안 → /ask-back 검토 화면과 실행 버튼 확인 |
| 최종 J13 DB 조회 | READ ONLY로 qualification_answers **0건**. 실행 버튼 누르지 않음 |

최종 후속 응답은 “현장 방문 완료, 확인서 미제출로 답변을 제안합니다. 증빙 보유 여부는 확인되지 않았습니다. 아직 저장하지 않았습니다...”이며 processing PASS, 후속 모델 호출0이다. 첫 질문은 실제 모델을 사용했다. 오프라인 테스트 작성 중 반환 타입을 잘못 참조한 테스트 실패도 보존하고 수정 후 전체 통과를 확인했다.

이번 사용자 승인으로 후속 합성 답변 전송 차단은 해소됐다. 모든 평가 환경 누적 추정 예약값 자체가 최종 $0.2312로 이번 $0.25 한도보다 낮다. 이는 제공자 확정 청구액이 아니다. 실패·재실행 비용도 예약에 포함됐다. 원문·응답·계정·화면은 Git에 넣지 않았다.

## 남은 한계와 평가

- 검토 화면이 현재 SOURCE-VISIT 및 normalized_value JSON을 그대로 표시한다. 채팅의 한국어 후보 안내는 개선됐지만, 상세 검토 화면의 필드별 한국어 표시 보완이 남았다. 화면에서 확인한 한계를 숨기지 않는다.
- final screenshot은 검토 카드와 실행 버튼이 보인 시점이다. 다른 상세 자료에는 로딩 문구가 남아 있어 전체 ask-back 화면 로딩 완료를 주장하지 않는다.
- 이번 J13 실제 사용자 사례에는 답변을 저장하지 않았다. 이 새 자연어 경로에서 명시 확인 후 저장·재판정까지의 전체 E2E는 미완료다. 기존 별도 DB 확인·재판정 회귀 통과로 이를 대신하지 않는다.
- 사용자에게 새 대화에서 질문 조회 → 같은 방문/제출 답변 → 서버 제안 검토까지 다시 평가받는다. Q-009 해석을 자동 확정하지 않는다.
