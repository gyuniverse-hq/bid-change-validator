# Q-016 출처 보존 및 후속 체크리스트 검증

2026-09-15, 시작 HEAD 730f678. 전용 로컬 평가 환경.

## 구현

이전에 의미 검증을 통과한 설명의 근거 원문을 ConversationState.document_memory에 보존한다. 모델의 결론이나 완료 플래그를 재사용하지 않는다. 다음 READ_DOCUMENT가 성공하고 현재 전체 문서 fingerprint와 사례·회사·버전·분석·판정 scope가 일치할 때 원문 Fact/Source를 중복 없이 보강한다. 답변 완료는 새 요청에 대해 다시 검증한다. 기존 tools.assert_fresh가 모델 처리 후에도 현재 fingerprint를 확인한다.

공고문 사용 중지, 조회 불가, 다른 회사/scope, 변경된 문서에서는 재사용하지 않는다. 검증 실패·원문 fallback만으로는 보존 항목을 만들지 않는다. 160KB 한도 초과는 보존 해제하며 잘린 자료를 완전한 기록처럼 쓰지 않는다. 대화는 여전히 프로세스 메모리이며 재시작 시 초기화된다. DB 스키마/공용 DB 변경 없음.

분류 제목도 검증 대상으로 명시했다. 필수 조건을 참고·협조로 낮추거나, 재직증명서와 신분증의 제출 관계를 택일로 바꾸거나, 보강된 원문에 있는 기한을 없다고 답하면 거절하도록 생성·검증 지침을 보완했다. 이 지침 자체가 정확도를 보장하지는 않는다.

## 자동 검사

`.ci-results/copilot-v31-20260915T064949Z/tests.xml`: 500 PASS. 기존 494개와 원문 보존 관련 6개. 기한 원문 보존·중복 제거·fingerprint/scope 변경·opt-out·unavailable·검증 실패와 원문 fallback 제외를 검사했다. `git diff --check` 통과.

## 추가 예산

사용자 추가 $10 승인 시점의 예약액 $6.0356을 별도 baseline으로 기록했다. 누적 로컬 한도 $16.03, 이번 추가 배정은 $9.9944로 $10 안이다. 기존 호출 기록을 보존했고 턴별 $0.25 상한 유지. 예약액과 실제 제공자 청구액은 다르며 사용자 확인 실제 지출 약 $3을 이 장부로 덮어쓰지 않는다.

## 실제 모델·Chrome 검증

`.ci-results/user-evaluation/job-followup-fix/chat-1789455063077/report.json`: 공개 공고문 ON, J05 동일 대화의 참여 준비→남은 일→체크리스트. 각각 76.288/47.827/74.017초, 모두 PARTIAL. 전체 검증 exit 1이며 성공으로 보고하지 않는다. JavaScript 오류 0, 차단된 쓰기 0, 전후 판정 목록 불변.

최종 체크리스트에서 전자 가격입찰 마감 2026-07-27 14:00와 방문접수 마감 15:00를 유지했고 과거 일정으로 표시했다. 공동수급·하도급 금지와 발표 참석자의 서류는 필수로 분류했으며, 재직증명서 + 신분증(종류 중 택일) + 사회보험 확인서의 관계도 보존했다. 이것은 이번 1회 실행의 개선 근거이며 일반 정확도 보장이 아니다.

남은 실패: MIXED_CHECK_ENTITIES, 권장 기록 보관을 원문 의무로 해석한 검증 거절, 일부 서류 설명 범위의 과장. 또 모델 검증은 ‘제안평가 종료 후’라는 개찰 시점을 이미 경과했다고 단정한 문장을 통과시켰다. 개발자 검토에서는 이를 미검증 시간 추론으로 남긴다. 개별 날짜가 없는 상대 일정은 기준 사건의 실제 완료가 확인되지 않으면 현재 대비 경과를 확정해서는 안 된다. 단순히 PARTIAL을 PASS로 바꾸지 않는다.

추가 승인 후 예약 증가분은 $0.3516 / $10이며 실제 청구액은 아니다. 이전 장부는 초기화하지 않았다.

현재 순서: Q-016 원문 보존·분류 보완 및 개발자 검증 수행 → 남은 설명 검증·상대 일정 추론 보완 → 전체 job 재검증 → 사용자 재평가·나머지 Core 교차평가. 아직 사용자 반복 테스트를 요청하지 않는다. 커밋 대상에 전체 공고 사본·계정·모델 응답은 포함하지 않는다.
