# 상대 일정과 검증된 본문 완료 판정

2026-09-15. 시작 HEAD `af149de`, 작업 브랜치 `codex/ai-copilot-evaluation-ready`.

## 변경

생성·검증 지침에 평가 종료 후/선정 후/개별 통보를 날짜 미확정 일정으로 구분하도록 명시했다. 다른 행사의 예정일 경과만으로 실제 행사 완료나 후속 일정의 경과를 추론하지 않는다. 이는 모델 지침이며 결과를 보장하지 않는다.

완료 여부는 초안 전체 문장 수 대신 검증된 본문과 사전에 고정한 완료 조건으로 판단한다. 모든 조건이 검증된 본문으로 충족되면 불필요한 미검증 추가 설명은 제외할 수 있다. 모순, 잘못된 근거, scope 불일치, 중복 ID, 화행 불일치, 보완 오류는 별도로 차단한다. 제외한 문장을 완료 근거로 사용할 수 없다. MIXED_CHECK_ENTITIES 문장을 검증 없이 허용한 변경이 아니다.

## 실행한 회귀검사

- `.ci-results/copilot-v31-20260915T070142Z`: 499 PASS / 3 FAIL. 초기 완료 조건 변경이 기존 안전 차단까지 완화하여 수정했다.
- `.ci-results/copilot-v31-20260915T070246Z`: 501 PASS / 1 FAIL. 버려진 보완 ID 이벤트를 실제 보완 실패와 구분했다.
- `.ci-results/copilot-v31-20260915T070337Z/tests.xml`: 502 PASS / 0 FAIL / 0 SKIP. 미검증 문장이 완료 근거가 될 수 없다는 두 경우를 추가했다.
- `git diff --check`: 통과.

## 실제 모델 세 턴 검사 — 전체 미통과

중단 전에 시작한 검사의 완성된 결과 파일을 확인했다. 중복 실행하지 않았다.
비공개 결과: `.ci-results/user-evaluation/job-followup-fix/chat-1789455889073/report.json`.

| 요청 | 응답 시간 | 서버 상태 |
| --- | --- | --- |
| 참여 준비 전체 | 62.839초 | PARTIAL |
| 남은 확인과 사용자 행동 | 42.162초 | PASS |
| 실행 체크리스트 | 62.163초 | PARTIAL |

HTTP 모두 200, 브라우저 오류 0, 차단된 업무 쓰기 0, 검사 전후 대상 판정 동일. 두 번째 요청의 PASS는 해당 응답 처리 상태이며 전체 job 통과가 아니다.

수동 대조에서 가격입찰 14시와 방문접수 15시 구분, 협조 요청 구분, 개찰 날짜 미확정 안내를 확인했다. 마지막 응답은 제안발표 예정일 경과와 실제 완료를 구분했으나, 설명회에는 여전히 ‘열렸고’라고 실제 개최를 추론한 표현이 남았다. 긴 나열식 본문도 남아 있다. 응답 속도 개선은 입증하지 못했다.

마지막 검증은 안전보건관리 이행 서약서의 제출자료 목록과 계약체결 시 의무를 함께 다루지 못한 분류를 거절했다. 이 실패는 통과로 바꾸지 않는다. 첫 요청의 생성 입력 54,024토큰, 검증 입력 79,025토큰이 기록되어 있다. 반복적인 전체 근거 전달은 속도 개선 조사 대상이며, 이것만으로 지연 원인을 확정하지 않는다.

이번 검사 예약 상한 추정액 증가 $0.3516, 누적 $6.7388, 설정 상한 $16.03. 실제 제공자 청구액과 다르다.

원래 남원 사례의 별도 보존 확인 명령은 재개 환경에서 Docker 실행 파일을 찾지 못해 실행되지 않았다. 이번에 그 별도 확인까지 통과했다고 보고하지 않는다. 위 판정 보존 결과는 완료된 J05 브라우저 검사에 한정한다.

## 다음 작업

④ 통합 검증 진행 중 → ⑤ 사용자 재평가 → ⑥ 최종 통합 판단.

다음 보완은 개별 문구 추가보다 제출 항목을 서류·제출 단계·기한·방법·필수/협조·근거로 묶는 방식에 집중한다. 같은 서류가 입찰과 계약 단계에 모두 등장하면 두 의무를 보존하고, 해당 단계별로 검증한 결과를 사용자 체크리스트로 표시한다. 예정일과 실제 완료도 별도 값으로 다룬다. 동일 세 턴에서 내용·반복·응답 시간을 함께 재검증한 뒤 사용자에게 재평가를 안내한다. 원문 충돌은 임의 해소하지 않는다.
