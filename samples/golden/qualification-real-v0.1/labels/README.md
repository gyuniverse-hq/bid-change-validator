# Labels are intentionally absent

Source / Canonical / Judgment / Change / Risk Ground Truth 파일: **0개**.
현재 관찰은 `cases/G2/observations/`에 있으며 정답이나 모델 성능 판정에 사용하지 않습니다.

향후 사람이 원문 quote/location과 document identity를 검수하고, label별로
`DRAFT → REVIEWED → APPROVED` 상태와 검수자·시점·provenance를 기록해야 합니다.
모델 출력 자체를 정답으로 승격하지 않습니다.
Source span과 Canonical Requirement, Profile별 Judgment, version pair별 Change,
9종 Contract Risk의 표현은 별도 PR에서 합의합니다. 이 저장 manifest는 그 스키마를 선점하지 않습니다.
