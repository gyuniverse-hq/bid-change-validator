# 현장 방문 자연어 답변의 제안 연결 검증

2026-09-15 KST, 검증 코드 a67b02d. 제품 코드는 변경하지 않았다.

## 전체 순서

1 감사 → 2 연결 → 3 기본 검사: 기존 기록 유지. 4 통합 검증: Q-009 기관 해석 대기. **5 사용자 평가: 질문 표시는 사용자 재확인, 자연어 답변 → 저장 전 제안 연결 실패 확인.** 6 최종 통합 판단: 대기.

## 실제 확인

- 사용자 질문: “현장 방문은 완료했지만 확인서는 아직 제출하지 않았어”. 사용자가 붙여넣은 결과는 기존 판정 설명이고 입력 제안이 없다.
- 전용 로컬 DB bid-copilot-evaluation-db/copilot_evaluation에서 READ ONLY로 해당 J13 사례 qualification_answers를 조회했다. **0건**. 현재 이 사례에 저장된 확인 답변은 없다. 과거 전체 쓰기 이력까지 증명하는 감사 로그 검사는 아니다.
- 서버 함수를 직접 실행했다. `_action_control=None`, `user_input=None`, `fallback_plan=[READ_CHECKS]`.
- 모델 gateway를 테스트 대역으로 두고 READ_CHECKS + PROPOSE_ACTION을 반환시켰다. `plan_turn` 결과는 **READ_CHECKS만 남았다**. 외부 모델 호출 없이 서버 필터가 제안을 제거함을 재현했다. 실제 모델 성공으로 보고하지 않는다.
- 원인: orchestration.py는 구조화 user_input 또는 저장·반영·재검증·취소 표현이 없는 경우 모델의 PROPOSE_ACTION을 제거한다. chat.py의 제한된 문법에도 이번 방문/제출 상태 문장은 없다. 자연어 두 상태를 확인 대상의 필드별 후보 입력으로 변환하는 연결이 없다.
- Chrome 두 턴 재현기는 Git 제외 로컬 폴더에 준비했지만, 자동 승인 검토에서 **후속 합성 답변 payload 전송 범위가 불명확하다**며 실행을 차단했다. 새 실제 모델 재현은 NOT_RUN이다. 우회 호출하지 않았다.

## 보완 방향과 남은 검사

현재 확인 질문에 대한 자연어 답변을 대상·필드별 후보값으로 추출하고, 현행 확인 계약과 대상 소유권·최신성을 검증한 뒤 저장 전 제안으로 보여주는 연결이 필요하다. 이 사례의 후보는 site_visited=true, visit_certificate=false이며 증빙 보유를 임의 추정하지 않는다. 모호한 답변이나 복수 대상은 재확인하고 실제 저장은 명시적인 실행 확인을 유지한다. 단순히 PROPOSE_ACTION 필터만 제거하면 구조화 값이 준비되지 않으므로 충분하지 않다.

후속 전송 승인 → 실제 2턴 재현 → 후보 입력 연결 보완 → 제안 전 무저장·확인 후 재판정·부정/모호성/다른 회사 경계 검사 → 사용자 재평가가 남았다. 이번은 원인 검증이며 수정 완료나 입력 E2E 통과가 아니다.
